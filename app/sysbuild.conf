#
# Ring•One Firmware · Dotstar Consulting · Apache 2.0
#
# Sysbuild configuration — tells the NCS sysbuild system to enable the
# nRF7002 Wi-Fi driver.  Without this, sysbuild forcibly sets
# CONFIG_WIFI_NRF70=n in the application image even when prj.conf requests it.
#

SB_CONFIG_WIFI_NRF70=y
